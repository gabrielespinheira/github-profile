import styled from 'styled-components'

export const Font = styled.div`
  position: fixed;
  top: 10px;
  left: 50%;
  z-index: -2;
  font-size: 13vw;
  font-weight: var(--bold);
  color: #f5f6fa;
  text-shadow: 0 -15px 100px rgba(0, 0, 0, 0.1);
  transform: translateX(-50%);
`
